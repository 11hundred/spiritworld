$('#new-post-event-form-wrap').html("<%= j (render 'form') %>");
$('#new-post-event-form-wrap').slideDown(350).promise().done(function() {
  applyPostEventTimeButtonActions();
  addEditLinkActions();
  $('html, body').animate({
    scrollTop: $('#new-post-event-form-wrap').position().top
  }, 500);
});
